#spring:
#  datasource:
#    url: jdbc:h2:mem:mejaiTest
#logging:
#  level:
#    org.hibernate.SQL : debug


spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/mejaiTest
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: create # 만들어지면 none
logging:
  level:
    org.hibernate.SQL: debug
    reactor:
      netty:
        http:
          client: debug
